import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.stringTheory",
  appName: "String Theory",
  webDir: "dist",
  bundledWebRuntime: false,
};

export default config;
